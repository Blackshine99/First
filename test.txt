git init
git add .
git commit -m "first"
git remote add origin <repo URL+ ".git">
git push -u origin master


config commands (one time only)
git config --global user.name "Your Name"
git config --global user.email "Your Email"
git config credential.helper store
